# Profile: quick — fast, cheap, read-only triage
# Install: cp quick.config.toml ~/.codex/   Use: codex --profile quick
model = "gpt-5.4-mini"
model_reasoning_effort = "low"
approval_policy = "never"
sandbox_mode = "read-only"
web_search = "disabled"
