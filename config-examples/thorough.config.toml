# Profile: thorough — deep work on hard problems
# Install: cp thorough.config.toml ~/.codex/   Use: codex --profile thorough
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "live"
